# Greenfield migration fixtures

These fixtures are synthetic and deterministic. They model redacted audit decisions without copying source CSV rows, personal email addresses, credentials, task links, or production URLs.

The deliberately sensitive supplier object uses only invalid example values so projection tests can prove that prohibited fields are dropped.
